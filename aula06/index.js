// Tipos Primitivos: String, undefined, null, boolean, syymbol

const nome = "Leo"; // string
const nome1 = 'Leo'; // string
const nome2 = `Leo`; // string
const num1 = 10; // number
const num2 = 10.53; // number
let nomeAluno; // undefined -> não aponta pra local nenhum da memória
const sobrenomeAluno = null; // Nulo -> não aponta pra local nenhum da memória
const aprovado = true /* ou false */ // Boolean = true or false (lógico)

console.log(typeof nome, nome) // tipo e valor
